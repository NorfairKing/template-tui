name:                foobar-tui
version:             0.0.0.0
license:             AllRightsReserved

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src
  dependencies:
  - async
  - brick
  - cursor
  - directory
  - envparse
  - mtl
  - optparse-applicative
  - path
  - path-io
  - text
  - vty
  - yaml
  - yamlparse-applicative

executables:
  foobar-tui:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - foobar-tui
